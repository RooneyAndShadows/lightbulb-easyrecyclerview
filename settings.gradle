pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        //noinspection JcenterRepositoryObsolete
        jcenter()
        google()
        maven { url 'https://jitpack.io' }
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        jcenter()
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "EasyRecyclerView"
include ':app'
include ':lightbulb-easyrecyclerview'
